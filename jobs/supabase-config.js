/**
 * Supabase project used by `/jobs/` form (publishable anon key — never commit service_role).
 * Dashboard → Project Settings → Data API → Project URL / Publishable API key (legacy anon).
 */
window.SAKURA_JOBS_SB = {
  url: '',
  anonKey: '',
};
